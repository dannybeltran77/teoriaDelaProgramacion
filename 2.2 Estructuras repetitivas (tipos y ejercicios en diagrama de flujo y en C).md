## 🔸 Ciclo mientras (while)

> while es una estructura de programación que ejecuta un bloque de código repetidamente mientras se cumpla una condición.

### Sintaxis

```c
while (condición) {
    // código que se repite mientras la condición sea verdadera
}

```

### 💻 Ejemplo

Escribe un programa que pida al usuario números enteros y los sume.
El programa debe continuar pidiendo números hasta que el usuario ingrese 0, momento en el que se mostrará la suma total de los números ingresados.


### Código implementado en C

```c
#include <stdio.h>

int main() {
    int numero, suma = 0;

    printf("Ingresa un numero (0 para terminar): ");
    scanf("%d", &numero);

    while (numero != 0) {
        suma += numero;
        printf("Ingresa otro numero (0 para terminar): ");
        scanf("%d", &numero);
    }

    printf("La suma total es: %d\n", suma);

    return 0;
}
```



### Compilación y ejecución del programa en C

<img width="900" height="2160" alt="Captura de pantalla (155)" src="https://github.com/user-attachments/assets/49a82df5-64a2-4d4c-8b73-3a57b5d3c5d3" />


### Representación del ejemplo en Diagrama de flujo

<p align="center">

<img width="259" height="525" alt="d5" src="https://github.com/user-attachments/assets/906efc0b-1ede-42d9-8198-915a5f58f8a8" />




## 🔸 Ciclo hacer mientras (do-while)

> do-while es un bucle que ejecuta primero el bloque de código y luego verifica la condición, garantizando que se ejecute al menos una vez.

### Sintaxis

```c
do {
    // código a ejecutar
} while (condición);

```

### 💻 Ejemplo

Realiza un programa en C que permita al usuario ingresar números enteros de forma repetida.
El programa debe sumar todos los números ingresados y finalizar cuando el usuario introduzca el número 0.
Finalmente, debe mostrar la suma total de los números ingresados (excluyendo el 0).

### Código implementado en C

```c
#include <stdio.h>

int main() {
    int numero, suma = 0;

    do {
        printf("Ingresa un numero (0 para terminar): ");
        scanf("%d", &numero);
        suma += numero;
    } while (numero != 0);

    printf("La suma total es: %d\n", suma);

    return 0;
}
```

### Compilación y ejecución del programa en C

<img width="900" height="2160" alt="Captura de pantalla (157)" src="https://github.com/user-attachments/assets/09571486-2ca4-4233-9350-cdc34e271ec4" />

### Representación del ejemplo en Diagrama de flujo

<p align="center">

<img width="281" height="439" alt="d6" src="https://github.com/user-attachments/assets/57982bb2-7d2c-453f-9afd-3736c8763681" />



## 🔸 Ciclo para (for)

### Sintaxis

```c
for (inicialización; condición; incremento) {
    // código a ejecutar en cada iteración
}
```

### 💻 Ejemplo

Escribe un programa que calcule la suma de todos los números pares del 1 al 20
e imprima también cuántos números pares se sumaron.

### Código implementado en C


```c

#include <stdio.h>

int main() {
    int suma = 0;
    int contadorPares = 0;

    for (int i = 1; i <= 20; i++) {
        if (i % 2 == 0) {  // Verifica si el número es par
            suma += i;
            contadorPares++;
        }
    }

    printf("La suma de los números pares del 1 al 20 es: %d\n", suma);
    printf("Cantidad de números pares sumados: %d\n", contadorPares);

    return 0;
}
```

### Compilación y ejecución del programa en C

<img width="900" height="2160" alt="Captura de pantalla (159)" src="https://github.com/user-attachments/assets/5a3a7df9-59bc-4e99-be70-993f50dcc9ad" />

### Representación del ejemplo en Diagrama de flujo

<p align="center">
<img width="438" height="687" alt="d7" src="https://github.com/user-attachments/assets/042236ac-1904-4a74-961a-cc415141ea97" />



## 🔸 Anidamiento de estructuras repetitivas (bucles anidados)

> Bucles anidados significa usar un bucle `for` dentro de otro bucle `for`.  
> 
> - El bucle externo controla un ciclo principal.  
> - El bucle interno se ejecuta completamente cada vez que se ejecuta una iteración del bucle externo.  
> - Se usa cuando necesitamos recorrer estructuras de dos dimensiones o hacer operaciones repetitivas dentro de otra repetición, como tablas, matrices o patrones.


# 📊 Ejemplo: Registro de asistencia semanal de empleados


Una empresa quiere registrar la asistencia de sus empleados durante la semana (5 días hábiles).

### ✅ El programa debe:

1. 👤 Pedir al usuario el **número de empleados**.
2. 📅 Para cada empleado, solicitar la **asistencia de cada día** (1 = presente, 0 = ausente).
3. 📈 Calcular el **total de días asistidos por cada empleado**.
4. 🏢 Calcular el **total de asistencias de todos los empleados**.

## Código implementado en C


```c
#include <stdio.h>

int main() {
    int empleados, dias = 5;
    int i, j;
    int asistencia;
    int totalEmpleado, totalGeneral = 0;

    printf("Ingrese el numero de empleados:\n ");
    scanf("%d", &empleados);

    for (i = 1; i <= empleados; i++) {
        totalEmpleado = 0;
        printf("\nEmpleado %d\n", i);

        for (j = 1; j <= dias; j++) {
            printf("  Dia %d (1=Presente, 0=Ausente):\n ", j);
            scanf("%d", &asistencia);
            totalEmpleado += asistencia;
        }

        printf("  Total de dias asistidos por empleado %d: %d\n", i, totalEmpleado);
        totalGeneral += totalEmpleado;
    }

    printf("\nTotal de asistencias de todos los empleados: %d\n", totalGeneral);

    return 0;
}

```
### Compilación y ejecución del programa en C

<img width="900" height="2160" alt="Captura de pantalla (175)" src="https://github.com/user-attachments/assets/97f726fd-5ea7-42bb-9d03-cddf47b4e74e" />
<img width="900" height="2160" alt="Captura de pantalla (174)" src="https://github.com/user-attachments/assets/72769fbc-5045-4d76-be6e-460b9219e345" />

### Representación del ejemplo en Diagrama de flujo


<p align="center">
<img width="431" height="916" alt="d8" src="https://github.com/user-attachments/assets/3b55dc97-a9e4-47c1-8c37-95a63509ab66" />


[⬅️ Página anterior](./2.1%20Estructuras%20condicionales%20(tipos%20y%20ejercicios%20en%20diagrama%20de%20flujo%20y%20en%20C).md) | [🏠 Ir al índice](./index.md) | [➡️ Página siguiente](./2.3%20Ejercicio%20combinando%20estructura%20condicional%20y%20repetitiva%20(Java%20o%20Python).md)



